
<%# 商品一覧画面 %>

<% layout('layouts/boilerplate') %>

<div class="mb-3">
    <% if (categoryName) { %>
        <h4 class="mb-3">「<%= categoryName %>」の商品</h4>
        <a href="/products" class="btn btn-sm btn-outline-secondary">フィルターを解除</a>
    <% } else { %>
        <h4>すべての商品</h4>
    <% } %>
</div>

<div class="row mb-3">

    <%# 商品が登録されていない場合 %>
    <% if (products.length === 0) { %>
        <div class="col-md-4">
            <p class="h4 text-muted fw-bold mb-5">現在販売中のアイテムがありません</p>
            <a href="/" class="btn btn-dark px-5 py-3">ホームへ</a>
        </div>
    <% } %>

    <% for(product of products) { %>
        <div class="col-6 col-md-4 col-lg-3 mb-5">
            <div class="card h-100 shadow-sm border-0 product-card">
                <a href="/products/<%= product._id %>" class="text-muted text-decoration-none">
                    <%# 画像部分 %>
                    <% if (product.images && product.images.length) { %>
                        <div class="ratio ratio-1x1 overflow-hidden">
                            <img src="<%= product.images[0].url %>" class="card-img-top object-fit-cover" alt="product-img">
                        </div>
                    <% } else { %>
                        <div class="ratio ratio-1x1 overflow-hidden">
                            <img src="/images/no-image.png" class="card-img-top object-fit-cover" alt="no-img">
                        </div>
                    <% } %>

                    <%# 商品情報 %>
                    <div class="card-body">
                        <p class="card-title text-dark fw-bold mb-1"><%= product.name %></p>
                        <p class="card-text text-dark fw-light">\ <%= product.price.toLocaleString() %></p>
                    </div>
                </a>
            </div>
        </div>
    <% } %>

    <%# ページングナビ %>
    <%- include('../partials/pagination') %>
</div>